---
const { width = 70, ...rest} = Astro.props as { width: number };
import LogoImg from "@/assets/images/logo.webp"
import { Image } from "astro:assets";
---

<Image src={LogoImg} alt="Logo de la cocina de Vero" width={width} class={"h-full"} {...rest}/>

