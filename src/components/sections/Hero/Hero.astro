---
import { Image } from "astro:assets";
import LogoImg from "@/assets/images/logo.webp";
import { useTranslations } from "@/lib/i18n/useTranslations";

const lang = Astro.currentLocale as "es" | "en" | "de";
const t = useTranslations(lang);
---

<section
  class="h-full min-h-[calc(100dvh-93px)] max-w-6xl gap-6 mx-auto grid grid-col-1 md:grid-cols-2 grid-rows-[auto_1fr] md:grid-rows-1 p-4 overflow-x-hidden"
>
  <div class="place-content-start md:place-content-center py-10">
    <h1 class="text-4xl md:text-6xl font-bold tracking-tighter">
      {t("hero.title").split(",")[0]}
      <br />
      <span class="text-primary">{t("hero.title").split(",")[1]}</span>
    </h1>
    <p class="pt-2 max-w-[75ch] text-neutral-500 md:text-xl">
      {t("hero.subtitle")}
    </p>
  </div>

  <div class="place-content-start md:place-content-center">
    <Image src={LogoImg} alt={"Logo de la cocina de Vero"} class={"scale-[1.2]"} priority={true}/>
  </div>
  <!-- <div class="place-content-start md:place-content-center">
    <div
      class="aspect-square md:aspect-auto md:h-[500px] w-full grid grid-cols-3 grid-rows-5 gap-4"
    >
      <div class="row-span-2">
        <Image
          src={camperoImage}
          alt="Plato de boquerones fritos"
          class={"w-full h-full object-cover rounded-md"}
        />
      </div>
      <div class="row-span-3 col-start-1 row-start-3">
        <Image
          src={polloAlCurryImage}
          alt="Plato de boquerones fritos"
          class={"w-full h-full object-cover rounded-md"}
        />
      </div>
      <div class="col-span-2 row-span-3 col-start-2 row-start-1">
        <Image
          src={potajeImage}
          alt="Plato de boquerones fritos"
          class={"w-full h-full object-cover rounded-md"}
        />
      </div>
      <div class="row-span-2 col-start-2 row-start-4">
        <Image
          src={rosquillasImage}
          alt="Plato de boquerones fritos"
          class={"w-full h-full object-cover rounded-md"}
        />
      </div>
      <div class="row-span-2 col-start-3 row-start-4">
        <Image
          src={pestinosImage}
          alt="Plato de boquerones fritos"
          class={"w-full h-full object-cover rounded-md"}
        />
      </div>
    </div>
  </div> -->
</section>
